import './App.css'
import {useSelector} from 'react-redux'

function App() {

   const state = useSelector(state => state.)
  return (
    <div className="App">
      <h2>Hello Redux ts</h2>
    </div>
  )
}

export default App
